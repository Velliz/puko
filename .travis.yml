language: php

sudo: false

php:
  - 5.4
  - 5.5
  - 5.6

cache:
    - apt

before_install:
 - composer --prefer-source install

script: phpunit --verbose tests/MainTest.php

notifications:
  email: "nurcholid@windowslive.com"

after_success:
- coveralls