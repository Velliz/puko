language: php

sudo: false

php:
  - 5.4
  - 5.5
  - 5.6

cache:
    - apt

before_install:
 - composer --prefer-source install

script: phpunit --bootstrap index.php Tests/Main

notifications:
  email: "nurcholid@windowslive.com"
